name: Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: install prereq
      run: pip install pipenv
    - name: Run the test script.
      run: make test
